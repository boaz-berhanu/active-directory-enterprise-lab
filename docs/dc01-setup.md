## Setting Up The First Domain Controller

Created `DC01` in VMWare using Windows Server 2025.

It was assigned 4 GB RAM, 2 CPU cores, and a 60 GB virtual disk.

Configured it with the static IP 10.10.10.10

I used a static IP because this server will become a domain controller and DNS server, so clients need to know where to find it.

